Zum aufsetzen vom backend:

```bash
cd mikurando-backend
npm install
```

Die Environment Datai (.env) ist im Gitignore, bitte selber erstellen.
Sie liegt unter "/mikurando-backend/.env", Alle Daten welche benötigt werden liegen
(ohne Pw) in ".env.template" dort bitte keine Passwörter rein (wird auf git getrackt)


Zum Starten des Servers
```
npm run dev 
```