/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrderReception } from './order-reception';

describe('OrderReception', () => {
  let component: OrderReception;
  let fixture: ComponentFixture<OrderReception>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [OrderReception]
    })
    .compileComponents();

    fixture = TestBed.createComponent(OrderReception);
    component = fixture.componentInstance;
    await fixture.whenStable();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
